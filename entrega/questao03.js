/* Crie uma função que retorna a soma dos elementos de um array. Exemplo:
    //soma = 1 + 2 + 3 + 4 + 5 = 15 */

    soma = [1, 1, 1, 2]
    

    function Total(){
        total=0
    for (let index = 0; index in soma; index++){
        total+=soma[index]
    }
} 
Total()
console.log(total)
   